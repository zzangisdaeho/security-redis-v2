package com.example.springsecurityredis.consulting;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ConsultingEntityRepository extends JpaRepository<ConsultingEntity, Long> {
}
